{
  "name": "delopay-shop",
  "version": "0.2.0",
  "private": true,
  "description": "Tailwind build for the DeloPay Shop WordPress theme.",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o assets/css/tailwind.css --minify",
    "watch": "tailwindcss -i src/input.css -o assets/css/tailwind.css --watch",
    "dev": "tailwindcss -i src/input.css -o assets/css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.9",
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss": "^3.4.17"
  }
}
